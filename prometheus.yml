global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'dns_shield'
    static_configs:
      - targets: ['dns-shield:5000']   # Use service name from docker-compose
    metrics_path: /metrics
    scrape_interval: 10s